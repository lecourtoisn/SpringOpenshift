spring:
  application:
    name: zuul
  profiles:
    active: prod
  cloud:
    config:
      uri: http://config-server.springopenshift.svc:8080

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-server.springopenshift.svc:8080/eureka